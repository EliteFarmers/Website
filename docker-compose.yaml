version: '3.8'
services:
    website:
        env_file:
            - .env
        environment:
            - NODE_ENV=production
        build: .
        ports:
            - 3000:3000
        depends_on:
            - database
            - cache
        restart: always
        networks:
            - elite-network
    cache:
        image: redis:7.0.7
        restart: always
        ports:
            - '6379:6379'
        command: redis-server --save 20 1 --loglevel warning --requirepass redisCachePassword123
        expose:
            - '6379'
        volumes:
            - redis-data:/data
        networks:
            - elite-network

networks:
    elite-network:
        driver: bridge

volumes:
    redis-data: {}
