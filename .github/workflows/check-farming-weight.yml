name: Check Farming Weight

on:
  push:
    branches: [main]
    paths:
      - 'packages/farming-weight/**'
  pull_request:
    paths:
      - 'packages/farming-weight/**'

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup
        uses: ./.github/actions/setup
      - name: Run tests
        run: pnpm -C packages/farming-weight test
  lint:
    name: Linting
    runs-on: ubuntu-latest 
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup
        uses: ./.github/actions/setup
      - name: Lint
        run: pnpm -C packages/farming-weight run lint
  build:
    name: Build
    runs-on: ubuntu-latest 
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup
        uses: ./.github/actions/setup
      - name: Build
        run: pnpm -C packages/farming-weight run build